#include<stdio.h>
int main(){
    int n,k;
	printf("The number of prisoners: ");
	scanf("%d",&n);
	printf("The number of prisoners to be skipped: ");
	scanf("%d",&k);	
    int ppl[n],count=n;
    for(int i=0;i<n;i++)
        ppl[i]=1;
    while(n>1){
        for(int i=)
    }
}